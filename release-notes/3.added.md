 New `key_patterns` argument: regular expressions matched against *key names* (the key-name analogue of `patterns`), so a single rule like `secret` can mask `secret`, `aws_secret_access_key`, ... without enumerating every variant. `keys` keeps its exact-match behaviour.