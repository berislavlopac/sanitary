`Sanitizer` only attempts `json.loads` on string values that start with `{` or `[` after stripping, instead of on every string; JSON objects/arrays are still walked, but bare JSON scalar strings (e.g. `"123"`) are no longer coerced to their parsed types.